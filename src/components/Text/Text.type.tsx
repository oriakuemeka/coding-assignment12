export interface TextProps {
  content: string; // Ensure 'content' is always a string
  backgroundColor?: string;
  color?: string;
  bold?: boolean;
  italic?: boolean;
  visible?: boolean;
  disabled?: boolean;
  small?: boolean;
  medium?: boolean;
  large?: boolean;
}
